import toast, { Toaster } from 'react-hot-toast';

export default function ToasterText({ text }) {
toast(text); 
    return (
        <div className="one">  
            
  console.log('API response total:', total);
            <Toaster />;
    </div>       );
}